@model BookDatabase.Models.UserSettingsDto

<script>
    window.shortcutMap = {
        home: '@Model.homeShortcut',
        books: '@Model.booksShortcut',
        about: '@Model.aboutShortcut',
        privacy: '@Model.privacyShortcut',
        settings: '@Model.settingsShortcut',
        darkMode: '@Model.darkModeShortcut',
        createBook: '@Model.createBookShortcut',
        searchbarFocus: '@Model.searchbarFocusShortcut',
        genreFilter: '@Model.genreFilterShortcut',
        ownershipFilter: '@Model.ownershipFilterShortcut',
        changeView: '@Model.changeViewShortcut'
    };
</script>
